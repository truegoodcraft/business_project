# BUS Core LazoralltheCore Goal Brief

This file restores the repo-root traceability target for agents that were
instructed to read `codex_goal.md` first.

The authoritative implementation brief for this scoped polish patch is
`codex_plan.md`.

Key boundaries remain:

- Implement the LazoralltheCore / Laser Everything Community Polish Patch only.
- Keep Core local-first, open-source, no forced cloud, and no telemetry.
- Do not add full POS, full accounting, accounting sync, automatic reorder,
  cloud sync, telemetry, full job scheduling, or Pro features.
- Do not implement FIFO drilldown, below-cost warning, Create Output Product
  shortcut, run notes, schema expansion, auth/write-gate changes,
  backup/restore trust changes, update/release authority changes, or a public
  `VERSION` bump without owner review.
- If governance requires a version change for meaningful control-surface work,
  bump only `INTERNAL_VERSION`.

Read `codex_plan.md` before implementation for the full scope, validation, and
output requirements.
