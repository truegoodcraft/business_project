[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tgc-alpha-core"
version = "0.8.8"
description = "Packaging stub for TGC Alpha Core."
readme = "README.md"
requires-python = ">=3.11"
