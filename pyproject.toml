[build-system]
requires = ["setuptools==84.0.0", "wheel==0.48.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tgc-alpha-core"
version = "1.4.1"
description = "Packaging stub for BUS Core."
readme = "README.md"
requires-python = ">=3.11"
