[pytest]
markers =
    unit: fast isolated tests of pure logic and narrow contracts
    api: HTTP/API contract tests using TestClient and isolated test DB
    integration: multi-component integration tests (filesystem + DB flows)
    smoke: high-level end-to-end smoke checks for critical user flows
    slow: longer-running tests excluded from default fast loops
    bus_dev(value): opt a fixture-backed test into explicit BUS_DEV behavior
