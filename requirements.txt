# External dependencies required for integrations and controllers.
notion-client>=2.2.0
google-api-python-client>=2.120.0
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
requests>=2.32.0
# Secret storage
keyring>=24.3.1
argon2-cffi>=23.1.0
cryptography>=43.0.0
# Cryptography utilities for plugin signing.
# Optional for plugin signature verification
# pynacl>=1.5,<2
# The Notion module otherwise relies on the Python standard library.
fastapi
uvicorn[standard]
pydantic
sqlalchemy
Send2Trash==1.8.2
pywin32>=306; platform_system == "Windows"

Jinja2>=3.1.4
reportlab>=4.2.0  # optional; only needed for fmt="pdf"
